## .dotfilesrc

DOTFILES_HOME="$(cd -P "$(dirname "$(readlink ~/.profile)")" && pwd)"
DOTFILES_ROOT_DIR="$(dirname "$DOTFILES_HOME")"
DOTFILES_ZSH_DIR="$DOTFILES_ROOT_DIR/zsh"
DOTFILES_BASH_DIR="$DOTFILES_ROOT_DIR/bash"

# vim:ft=sh
