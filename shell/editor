# Editor
# ~~~~~

EDITOR='vim'
VISUAL='vi'
LESS='-iMR'
PAGER='less'
#LESSCHARSET=latin1

if [ ! -x $(which vim 2>/dev/null) ]; then
    EDITOR='vi'
    VISUAL='vi'
fi

if which lesspipe 2>/dev/null; then
    eval $(lesspipe)
fi

export EDITOR VISUAL LESS PAGER

# vim:ft=sh
