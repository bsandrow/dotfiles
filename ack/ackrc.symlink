# search all text files
-a
--nobinary

# deterministic file ordering in output
--sort-files

# color even if piped somewhere (override with --no-color if needed; last one
# wins)
--color

# we want to follow symlinks
--follow

