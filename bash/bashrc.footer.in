#----------------------------------------
# Local Settings
#----------------------------------------
LOCAL_BASHRC="$HOME/.local/etc/bash/bashrc"
if [ -f "$LOCAL_BASHRC" ]; then
    source "$LOCAL_BASHRC"
fi

# vim:set ft=sh:
