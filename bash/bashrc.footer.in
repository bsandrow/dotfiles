#----------------------------------------
# Local Settings
#----------------------------------------
LOCAL_BASHRC="$HOME/.bashrc.local"
if [ -f "$LOCAL_BASHRC" ]; then
    source "$LOCAL_BASHRC"
fi

# vim:set ft=sh:
